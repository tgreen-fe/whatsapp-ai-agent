PORT=3000

# Twilio WhatsApp webhook security (optional but recommended)
TWILIO_AUTH_TOKEN=your_twilio_auth_token

# OpenAI
OPENAI_API_KEY=your_openai_api_key
# Must be a model that supports the Responses API web_search tool (gpt-5.4-mini does).
OPENAI_MODEL=gpt-5.4-mini